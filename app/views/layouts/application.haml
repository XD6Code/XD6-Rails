!!! 5
%html
    %head
        %title= h yield(:title)
        = stylesheet_link_tag  "application", media: "all"
        = javascript_include_tag "application"
        = csrf_meta_tags
    %body
    .container
        .row
            .col-12
                .navbar
                    %a.navbar-brand(href="/")
                        XD6{
                        %span.commentcolor
                            \//Code
                        }
                    %ul.nav.navbar-nav.pull-right
                        %li
                            = link_to "Home", "/", :class => action_name == "home" ? "active" : ""
                        %li
                            = link_to "About", "/about", :class => action_name == "about" ? "active" : ""
                        %li
                            = link_to "News", "/news", :class => action_name == "news" ? "active" : ""
                        %li
                            = link_to "Contact", "/contact", :class => action_name == "contact" ? "active" : ""

        .row
            = yield
